# Generated outputs

Training and evaluation outputs are written below `Results/Experiments/<run-name>/`.

The generated tree contains UTF-8 logs, run status, configuration snapshots, split manifests, checkpoints, metric histories, evaluation summaries, and optional prediction arrays. Generated content is excluded from version control by default.
